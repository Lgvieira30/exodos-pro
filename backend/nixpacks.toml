[phases.install]
cmds = ["npm ci", "cd ../frontend && npm ci"]

[phases.build]
cmds = ["cd ../frontend && npm run build", "cd ../backend && npm run build", "cp -r ../frontend/dist ./public"]

[start]
cmd = "node dist/server.js"
